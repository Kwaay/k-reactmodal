import { ReactElement } from 'react';
import { ModalProps } from './Modal.types';
declare function Modal({ type, title, content, width, height, position, delay, }: ModalProps): ReactElement;
export default Modal;
